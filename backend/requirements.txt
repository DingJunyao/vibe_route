# FastAPI 和服务器
fastapi
uvicorn[standard]
python-multipart
slowapi

# 数据库
sqlalchemy
alembic

# 数据库驱动 (支持多数据库)
aiosqlite
asyncmy  # x86 平台推荐（高性能），ARM 需 Rust 编译
aiomysql  # ARM 平台备用（纯 Python）
asyncpg  # PostgreSQL 异步驱动，ARM 需 Rust 编译

# Alembic 同步驱动 (用于数据库迁移)
pymysql
psycopg2-binary  # PostgreSQL 同步驱动，ARM 需编译

# 认证
bcrypt==4.0.1
python-jose[cryptography]
passlib[bcrypt]
python-dotenv

# 任务队列
celery
redis

# 验证
pydantic
pydantic-settings
email-validator

# HTTP 客户端
httpx
aiofiles
requests

# gpxutil 核心依赖
gpxpy
lxml
pandas
geopandas
shapely

# SVG 生成
svgwrite
svgpathtools @ https://github.com/bcwhite-code/svgpathtools/archive/refs/heads/master.zip
fonttools

# 图像处理
pillow
cairosvg
imageio
numpy

# 其他工具
tqdm
pyyaml
pypinyin
loguru
openpyxl

# 测试
pytest
pytest-asyncio
httpx

# 解压
rarfile
playwright==1.48.0
