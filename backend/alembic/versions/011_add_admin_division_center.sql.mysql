-- 011: 添加行政区划中心点和子级数量字段 (MySQL 版本)
--
-- 为 AdminDivision 模型添加以下字段：
-- - center_lon: 中心点经度（浮点数）
-- - center_lat: 中心点纬度（浮点数）
-- - children_num: 子级数量

ALTER TABLE admin_divisions
    ADD COLUMN center_lon DOUBLE NULL COMMENT '中心点经度',
    ADD COLUMN center_lat DOUBLE NULL COMMENT '中心点纬度',
    ADD COLUMN children_num INT NULL COMMENT '子级数量';
